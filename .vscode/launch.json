{
    "configurations": [
        {
            "type": "node",
            "name": "Run Script: runner-dev",
            "request": "launch",
            "command": "npm run runner-dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}